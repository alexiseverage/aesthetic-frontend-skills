---
slug: xlyk
label: Xlyk
first_researched: "2026-07-17"
last_updated: "2026-07-17"
source: unknown
image_count: 0
evidence_level: limited
new_aesthetic: true
aliases: []
---

# Xlyk

Origin, date, authorship, lineage, and source record are unknown.

## Dimension Synthesis

_The tiers below express canonical intent rather than observed image frequency._

| Dimension | Canonical (≥70%) | Common (30–70%) | Variant (<30%) |
|---|---|---|---|
| Palette | black and off-black ground; muted gray structure; alien green active channel; explicit semantic pairing | cyan, violet, orange, and red signal channels; analog neutrals | warm off-white exposed layer; temporary full-channel split |
| Type | terminal or programming-style monospace; tabular figures; stable log columns | pixel, bitmap, LCD, dot-matrix, and seven-segment values | glitch, HUD, sci-fi interface, or digital-anime display identifier |
| Texture | scanlines, phosphor grain, low-opacity noise, character-cell density | RGB offsets, moiré bands, oscilloscope persistence | strong glitch burst during state change |
| Shape | dominant concentric fields, reticles, interrupted arcs, closure gaps, proximity clusters, continuous paths | repeated sacred geometry, alchemical marks, Hermetic seals, correspondence diagrams, state micrographics | deep figure-ground reversal or complex star polygon |
| Motion | radar and oscilloscope sweeps; ring pulses; live counters; typewriter reveals | scan distortion, channel splitting, ASCII dissolve, self-drawing symbols | moiré ripple and peripheral-drift shimmer |
| Spatial | dominant perceptual-data canvas with metric, plot, and log support | layered inspectors, correlation grids, heatmaps, timelines | dense overlapping evidence field |
| Cultural markers | unresolved records, UFO traces, unknown-contact data, scientific mystery, sacred micrographics | extra-terrestrial hypotheses, redactions, conspiracy annotations | explicit specimen silhouette or recovered fragment |

## Image Descriptions

_No image corpus was collected. Origin and source record are unknown._

## Analysis

_Analyzed: 2026-07-17 | Images reviewed: 0 | Analyst: canonical synthesis_

### Color

| Role | Value | Use |
|---|---|---|
| Void | `#050606` | outer canvas and deepest instrument field |
| Ground | `#0B0E0D` | primary application surface |
| Graphite | `#1C211F` | panels and inactive modules |
| Muted | `#58605A` | grid, dividers, disabled states |
| Analog | `#B7B6A9` | secondary copy and neutral traces |
| Paper | `#D5D0BD` | exposed labels and high-contrast text |
| Active | `#A8FF3E` | acquisition and confirmed state |
| Reference | `#39D8FF` | baseline and comparison channel |
| Anomaly | `#A66BFF` | uncertain or unexplained state |
| Caution | `#FF6A3D` | warning and unstable state |
| Breach | `#FF355B` | failure, loss, or destructive state |

Use `70%–88%` neutral ground, `8%–20%` analog structure, and `4%–12%` signal color. Every signal color receives a label plus a unique point shape or line pattern.

### Shape & Perception

- Primary ring field: three to seven concentric bands; `1–4px` strokes; one to three closure gaps per band; occupy `18%–36%` of a major viewport or recur as at least three visibly related smaller fields.
- Radar sectors: `18°–54°`; opacity `8%–22%`.
- Reticle ticks: `4–14px`; radial intervals of `5°`, `10°`, or `15°`.
- Proximity grouping: `4–8px` within groups and `16–24px` between groups.
- Figure-ground reversal: one controlled inversion per major module; never invert paragraph text.
- Peripheral drift: alternating low-contrast segments below `3:1`, confined to a ring or margin.
- Sacred micrographics: `24–96px`; `1–2px` strokes; three to seven related marks per major view; consistent state meaning across the application.
- Correspondence diagram: one `96–180px` construction per major view, combining rings, polygons, paths, indexed nodes, and closure gaps to reveal an actual relationship or state.
- Gestalt linkage: repeat at least two of closure, continuation, similarity, proximity, common direction, or figure-ground reversal across separate components so the view reads as one perceptual system.

### Typography

- Primary monospace: `400–600`, `12–16px`, tabular figures, stable character width.
- Pixel or bitmap: `10–16px`, integer scaling, short labels only.
- LCD, dot-matrix, or seven-segment: `32–88px`, counters and measured values only.
- Glitch, HUD, sci-fi interface, or digital-anime display: `24–56px`, one identifier per viewport.
- Maximum face categories per viewport: three.

### ASCII Integration

- Valid uses: chart fills, density maps, sparklines, separators, component masks, loading states, log gutters, and dissolve transitions.
- Wide line limit: `120` characters; compact line limit: `64` characters.
- Decorative nodes: `aria-hidden="true"`; equivalent state is exposed through text, values, or structured data.
- Invalid use: standalone panel, route, card, headline, showcase, or background mural.

### Data Visualization

- Each primary surface combines at least three of: metric bank, statistical plot, event log, waveform, heatmap, correlation grid, radial plot, relationship map, counter, or timeline.
- Legends sit outside plot marks and remain visible at `200%` zoom.
- Numeric columns use stable widths and explicit units.
- Color channels also differ by point shape, line style, fill pattern, or label.
- Complex canvas data receives a table or log alternative.

### Motion

- Scan or glitch burst: `80–160ms`; maximum three rapid changes per second.
- RGB split: `80–140ms`; `1–3px` offset; no continuous split behind text.
- Typewriter: `18–45ms` per glyph; allow instant completion.
- ASCII dissolve or morph: `240–520ms`, `steps(6–14, end)`.
- Radar or oscilloscope sweep: `900–2400ms linear`.
- Ring pulse: `900–1800ms`, scale `1 → 1.08`, opacity `0.65 → 0`.
- Moiré ripple: `1200–3000ms linear`, confined to the data field.
- Peripheral-drift shimmer: `1600–4000ms`, low contrast, optional.
- Self-drawing symbol: `600–1600ms` via `stroke-dashoffset`.
- Live ticker or counter: `80–180ms` stepped replacement with stable width.
- Reduced motion: show final static frames; remove all continuous, illusory, split-channel, flicker, sweep, and morph behavior.

### Layout

- Desktop: dominant field `minmax(0, 1fr)` with `200–260px` metric rail and `280–360px` log or inspector rail; reserve a conspicuous `18%–36%` viewport region for perceptual geometry.
- Compact: a prominent ring or correspondence field appears within the first two viewport lengths, followed by counter bank, multichannel plot, event log, and stable action rail.
- Baseline spacing: `8px`; component gap `8–16px`; outer gap `24–40px`.
- Perceptual patterns use `pointer-events: none` and never overlap focus rings or input hit areas.

## Connections

- `glitch` overlaps in distortion and channel separation; Xlyk binds those effects to state changes.
- `j-gov-futurism` overlaps in dense instrumentation; Xlyk adds psychophysics and sacred symbolism.
- `techno-noir` overlaps in dark analytical surfaces; Xlyk adds multichannel semantic data and illusory motion.
- `cybersigilism` overlaps in symbolic linework; Xlyk treats symbols as indexed micrographics.
- `high-performance-hmi` overlaps in operational hierarchy; Xlyk permits speculative records and perceptual fields.

## Research Updates

_2026-07-17 — Initial source-free synthesis. Origin and source record remain unknown._
